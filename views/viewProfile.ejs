<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PasTime | Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
</head>

<body class="bg-light">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">PasTime</a>

            <div>
                <a href="/" class="btn btn-outline-primary me-2">Home</a>
                <a href="/logout" class="btn btn-primary">Log Out</a>
            </div>
        </div>
    </nav>

    <!-- Profile Content -->
    <main class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow-sm p-4">
                    <h3 class="text-center mb-4">Your Profile</h3>

                    <!-- Profile Fields -->
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between">
                            <strong>Email:</strong>
                            <span><%= user.email %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <strong>Username:</strong>
                            <span><%= user.username %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <strong>First Name:</strong>
                            <span><%= user.userFirstName %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <strong>Last Name:</strong>
                            <span><%= user.userLastName %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <strong>State:</strong>
                            <span><%= user.state %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <strong>City:</strong>
                            <span><%= user.city %></span>
                        </li>
                    </ul>

                    <div class="text-center mt-4">
                        <a href="/editProfile" class="btn btn-primary px-4">Edit Profile</a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="text-center py-4 bg-light">
        <small class="text-muted">&copy; 2025 PasTime</small>
    </footer>
</body>
</html>